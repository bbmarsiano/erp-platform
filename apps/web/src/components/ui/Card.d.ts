interface CardProps {
    children: React.ReactNode;
    padding?: number;
    style?: React.CSSProperties;
}
export declare function Card({ children, padding, style }: CardProps): import("react/jsx-runtime").JSX.Element;
export {};
