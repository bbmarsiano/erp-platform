{
  "name": "@dflow/finance",
  "version": "1.0.0",
  "main": "dist/module.plugin.js",
  "scripts": {
    "build": "esbuild module.plugin.ts manifest.ts 'src/**/*.ts' --platform=node --format=cjs --outdir=dist --outbase=."
  },
  "dependencies": {
    "@dflow/core": "workspace:*",
    "@dflow/db": "workspace:*",
    "pdfmake": "^0.2.20"
  }
}
