name: Deploy to Vercel

# Vercel is connected directly to GitHub and auto-deploys on push to main.
# This workflow is intentionally disabled to avoid conflicts.
# Manual deploys can be triggered from the Vercel dashboard.

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy frontend (manual)
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deploy is handled automatically by Vercel GitHub integration"

